# LangGraph搭載 自律型リサーチエージェント - 依存関係
# Python 3.10以上が必要です

# LangChain & LangGraph
# 注意: langchain-coreはlangchainの依存関係として自動インストールされますが、
# 直接インポートしているため明示的に記載しています
langchain>=0.1.0,<2.0.0
langgraph>=0.2.0,<2.0.0
langchain-openai>=0.1.0,<1.0.0
langchain-google-genai>=1.0.0,<5.0.0  # Gemini対応（v4.0.0以降に対応）
langchain-community>=0.0.20,<1.0.0
langchain-core>=0.1.0,<3.0.0
langchain-tavily>=0.1.0  # Tavily検索統合

# 検索API
tavily-python>=0.3.0

# データモデル & 設定
pydantic>=2.0.0,<3.0.0
pydantic-settings>=2.0.0,<3.0.0

# Webスクレイピング
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0  # BeautifulSoup4のパーサーとして推奨
trafilatura>=1.6.0  # 記事コンテンツ抽出用

# リトライ & エラーハンドリング
tenacity>=8.2.0

# 環境変数管理
python-dotenv>=1.0.0

# Redis (オプション)
redis>=5.0.0

# Database
sqlalchemy>=2.0.0,<3.0.0
psycopg2-binary>=2.9.9
alembic>=1.12.0

# API
fastapi>=0.104.0,<1.0.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6  # FastAPIでファイルアップロードを使用する場合に必要

# GUI
streamlit>=1.28.0

# PDF生成
reportlab>=4.0.0
PyPDF2>=3.0.0  # PDFファイルからのテキスト抽出用（オプション）

# テスト
pytest>=7.4.0,<10.0.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
pytest-asyncio>=0.21.0

# 開発ツール
black>=23.11.0
flake8>=6.1.0
mypy>=1.7.0
ruff>=0.1.0  # 高速なリンター・フォーマッター（オプション）
